<?xml version="1.0" encoding="utf-8"?>
<!--we use layout for data binding purposes-->
<!--<layout-->
        <!--xmlns:android="http://schemas.android.com/apk/res/android"-->
        <!--xmlns:app="http://schemas.android.com/apk/res-auto"-->
        <!--xmlns:tools="http://schemas.android.com/tools">-->

    <!--we create data which is the binding data-->
    <!--<data>-->
        <!--<variable name="model" type="apps.android.fattahnexx103.kotlinapp.model.model"/>-->
    <!--</data>-->

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            tools:context=".view.DetailFragment">


        <ImageView
                android:id="@+id/detail_imageView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:scaleType="center"
                android:layout_marginHorizontal="8dp"
                android:layout_marginBottom="32dp"
                android:minHeight="200dp"/>

        <TextView
                android:id="@+id/itemName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{model.name}"
                android:textStyle="bold"
                android:textSize="28sp"
                android:layout_margin="8dp"
        />

        <TextView
                android:id="@+id/itemLocation"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{model.location}"
                android:textSize="22sp"
                android:layout_margin="8dp"
        />

        <TextView
                android:id="@+id/itemSpeed"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{model.speed}"
                android:textSize="22sp"
                android:layout_margin="8dp"
        />

        <TextView
                android:id="@+id/itemDiet"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{model.diet}"
                android:textSize="22sp"
                android:layout_margin="8dp"
        />


    </LinearLayout>

<!--</layout>-->